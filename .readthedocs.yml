build:
  image: latest

python:
  version: 3.6
  setup_py_install: false

submodules:
  include: []
  recursive: false

python:
  setup_py_install: true
  pip_install: true
  extra_requirements:
      - tests
      - docs